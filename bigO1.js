function linearFunc(arr){
  for(let i =0; i < arr.length; i++){
    console.log(1000*100);
    let something = (50 * 20)/ 5;
    console.log(something);
  }
}

linearFunc([1,2,3,4,5]);

//O of 1

// for loop is O of 'arr' because it's however much arr is. aka O(n)
